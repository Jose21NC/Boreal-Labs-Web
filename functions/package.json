{
  "name": "boreal-labs-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "build": "echo 'JS functions: no build step'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint . || true"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.8.0",
    "pdf-lib": "^1.17.1",
    "qrcode": "^1.5.4",
    "@pdf-lib/fontkit": "^1.1.1"
  }
}
