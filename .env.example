# Copia este archivo a `.env` en la raíz del proyecto (no lo subas al repo)
# Correos admin permitidos (separados por coma). Deben existir como usuarios en Firebase Auth (Email/Password habilitado).
VITE_ADMIN_EMAILS=admin@borealabs.org,otro@borealabs.org
